<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:circular="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main_layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/bglayout_off"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".MainActivity">

    <!--android:background="#222B30" -->
    <kr.opid.app.flash.CircularProgressBar
        android:id="@+id/batteryprogressbar"
        style="@style/Widget.ProgressBar.CircularProgressBar"
        android:layout_width="150dip"
        android:layout_height="150dip"
        android:layout_centerHorizontal="true"
        android:layout_centerInParent="true"
        android:layout_centerVertical="true"
        android:layout_marginTop="10dip"
        circular:hasShadow="true"
        circular:strokeWidth="35"
        circular:subtitle="battery remaining"
        circular:title="Title" />

    <TextView
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_above="@+id/batteryprogressbar"
        android:layout_alignParentEnd="true"
        android:gravity="center"
        android:paddingBottom="10dp"
        android:text="@string/manual"
        android:textColor="#D1D1D1" />


</RelativeLayout>